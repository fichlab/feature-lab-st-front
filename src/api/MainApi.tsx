// import axios from 'axios';

// const BASEURL = '/';

// export const createGetRequest = async (endpoint: string, params?: any) => {
//   const response = await axios.get(`${BASEURL}${endpoint}`, {
//     params: { ...params },
//   });
//   return response.data.data;
// };

// export const createPostRequest = async (endpoint: string, body: any, params?: any) => {
//   const response = await axios.post(`${BASEURL}${endpoint}`, body, {
//     params: { ...params },
//   });
//   return response.data.data;
// };
